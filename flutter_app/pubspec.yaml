name: ekthikana
description: EkThikana — one place for everything.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  http: ^1.6.0
  file_picker: ^12.0.0
  pdfrx: ^2.4.7
  intl: ^0.20.3
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
