"""Database layer (PostgreSQL via SQLAlchemy).

Phase 2 migration: this package owns the SQLAlchemy engine, declarative base,
and the CommuteDB ORM models. Firestore-backed data is intentionally NOT
modelled here — that work is deferred per the Phase 2 brief.
"""
